{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open --host",
    "build": "vite build",
    "preview": "vite preview --open --host"
  },
  "devDependencies": {
    "bootstrap-icons": "^1.8.3",
    "sass": "^1.53.0",
    "vite": "^2.9.9"
  }
}
